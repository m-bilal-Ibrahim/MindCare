"""
Client for object/file storage.

Eventually this module will wrap upload/download/signed-URL calls for
storing files (e.g. reports, attachments) used across apps, keeping
the choice of storage backend isolated behind this one client.
"""

# Architecture

MindCare Backend is a modular monolith: a single Django project (`config`) containing
one Django app per product domain under `apps/`, plus two shared, non-app modules
(`core/` and `integrations/`) used across those apps. See @docs/decisions.md for the
reasoning behind this structure.

## Project layout

```
config/            Django project: settings (base/dev/prod/test), urls.py, celery.py
apps/               One Django app per domain (see "Modules" below)
core/               Shared, non-app code: exceptions, permissions, audit, encryption, pagination
integrations/       Outbound clients for third-party services
requirements/       base / dev / prod pip requirement sets
docs/               This file, decisions.md, deployment.md
```

## Modules (apps/)

- **accounts** — user accounts, authentication, roles (patient / psychologist / admin).
- **patients** — patient profiles and patient-specific data.
- **psychologists** — psychologist profiles, credentials, and their assigned patients.
- **appointments** — scheduling and managing therapy sessions (incl. Zoom integration).
- **clinical_notes** — psychologist-authored notes on patients/sessions (PHI).
- **journals** — patient-authored journal entries (PHI).
- **recommendations** — AI-drafted recommendations that must pass psychologist approval
  before reaching a patient (see the hard rule in @CLAUDE.md).
- **wearables** — ingestion of wearable/health device data.
- **reports** — generated reports/exports derived from other apps' data.
- **payments** — billing and payments (Stripe integration).
- **notifications** — push/email notifications (FCM integration).
- **communities** — peer community spaces/discussions.
- **moderation** — moderation of community/journal/communication content.
- **ngo** — NGO partner accounts and related workflows.
- **rewards** — gamification/rewards for patient engagement.
- **emergency** — crisis/emergency escalation flows.

## Shared modules

- **core/** — not a Django app; cross-cutting code every app may import:
  `exceptions.py`, `permissions.py`, `audit.py`, `encryption.py`, `pagination.py`.
- **integrations/** — outbound API clients, one subpackage per third-party service:
  `ai_service/` (the separate FastAPI inference service), `stripe_client/`,
  `zoom_client/`, `fcm_client/`, `storage_client/`.

## Per-app internal structure

Every app under `apps/` follows the same internal layout:

```
models.py                  ORM models
services.py                 Write-path business logic
selectors.py                 Read-path query logic (incl. ownership filtering)
permissions.py              App-specific DRF permission classes
tasks.py                     Celery tasks
api/serializers.py
api/views.py                 Thin views: parse -> service/selector -> serialize
api/urls.py                  Mounted under /api/v1/<app-name>/ in config/urls.py
tests/test_services.py
tests/test_api.py
```

## Request flow

A request hits `config/urls.py`, is routed to an app's `api/urls.py`, handled by a
view in `api/views.py` that delegates to that app's `services.py` (for writes) or
`selectors.py` (for reads), and the result is serialized by `api/serializers.py`.
Business logic never lives in the view itself.

## Async work

Celery (`config/celery.py`), broker and result backend on Redis, is used for
background work (e.g. sending notifications, processing wearable data). Each
app's `tasks.py` defines its own Celery tasks.

## API docs

Schema generated by drf-spectacular, served at `/api/docs/`.
